server {
    listen 80;

    server_name ssr.reactedge.net;

    location / {
        proxy_pass http://widgets-ssr:3001;

        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}